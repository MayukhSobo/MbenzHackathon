# Stage status to start server:
#   - "dev", for start server without graceful shutdown
#   - "prod", for start server with graceful shutdown
STAGE_STATUS="dev"

# Server settings:
SERVER_HOST="0.0.0.0"
SERVER_PORT=9999
SERVER_READ_TIMEOUT=60

# Internal API settings:
VEHICLE_INFO_ENDPOINT="http://localhost:9000/api/internal/v1/status/"
POC_DISTANCE_ENDPOINT="http://localhost:9000/api/internal/v1/distance/"
POC_STATIONS_ENDPOINT="http://localhost:9000/api/internal/v1/stations/"